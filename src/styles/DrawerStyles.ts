import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  logoutButton: {
    marginRight: 12,
    paddingVertical: 8,
    paddingHorizontal: 12,
  },
  logoutText: {
    color: '#1f6feb',
    fontWeight: '600',
  },
});

